# -*- mode: snippet -*-
# name: bit subset for loop
# key: bitsubset
# --
for (int bit = 0; bit < (1 << n); ++bit) {
  for (int A = bit; 0 <= A; --A) {
    A = A & bit;
    int subset = bit - A;
  }
}
